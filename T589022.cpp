#include <bits/stdc++.h>
using namespace std;
typedef long long LL;
const int INF = 0x3f3f3f3f;
const int N = 1e5 + 1;
const int M = 1e3 + 1;
const int MOD = 998244853;
int n, m, a[N], p[N], dp[N];
int main () {
    cin.sync_with_stdio(0);

    cin >> n >> m;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    for (int i = 1; i <= m; i++)
        cin >> p[i];
    
    return 0;
}